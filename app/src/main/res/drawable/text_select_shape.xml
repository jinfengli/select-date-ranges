<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">
    <solid android:color="@color/calendar_selected_radius_bg" />
    <padding
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp" />
    <solid android:color="@color/calendar_selected_radius_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/calendar_selected_radius_bg" />
    <size
        android:width="36dp"
        android:height="36dp" />
</shape>